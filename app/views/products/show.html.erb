<h1>Product:
    <%= @product.product_name %>
</h1>

<p><%= @product.description %></p>
    <p>Units Available: <%= @product.quantity %>
    <br> Seller: <%= @seller.first_name%> <%= @seller.last_name%>
    <br> Price: $<%= @product.price%>/item
    <br> Local Delivery Available? 
    <% unless @product.local %>
        No
        <% else %>
        Yes
        <% end %>
    </p>

    <button type="button" class="btn btn-primary">
            <%= "Add to Order" %>
    </button>